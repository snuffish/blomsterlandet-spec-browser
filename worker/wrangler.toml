name = "blomsterlandet-stock"
main = "src/index.ts"
compatibility_date = "2026-09-23"

# No KV, no Cache API, no durable state: this Worker deliberately holds nothing.
# Stock is read live on every request.
